<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/colorSurface"
    android:padding="16dp">

    <!-- Drag handle -->
    <View
        android:id="@+id/dragHandle"
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:background="@drawable/bottom_sheet_handle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Station icon -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardIcon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        app:cardBackgroundColor="@color/primary"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dragHandle">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:contentDescription="@string/bus_stop_icon"
            android:src="@drawable/ic_bus_stop"
            app:tint="@color/white" />

    </com.google.android.material.card.MaterialCardView>

    <!-- Station name -->
    <TextView
        android:id="@+id/textViewStationName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:textColor="?attr/colorOnSurface"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/buttonFavorite"
        app:layout_constraintStart_toEndOf="@id/cardIcon"
        app:layout_constraintTop_toTopOf="@id/cardIcon"
        tools:text="Avtobusna postaja" />

    <!-- Station number -->
    <TextView
        android:id="@+id/textViewStationNumber"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="@id/textViewStationName"
        app:layout_constraintStart_toEndOf="@id/cardIcon"
        app:layout_constraintTop_toBottomOf="@id/textViewStationName"
        tools:text="Postaja št. 001" />

    <!-- Favorite button -->
    <ImageButton
        android:id="@+id/buttonFavorite"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/favorite"
        android:src="@drawable/ic_favorite_border"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/cardIcon"
        app:tint="@color/primary" />

    <!-- Departures section -->
    <TextView
        android:id="@+id/textViewDeparturesLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/departures"
        android:textColor="?attr/colorOnSurface"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cardIcon" />

    <!-- Loading indicator -->
    <ProgressBar
        android:id="@+id/progressBarDepartures"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/textViewDeparturesLabel"
        app:layout_constraintStart_toEndOf="@id/textViewDeparturesLabel"
        app:layout_constraintTop_toTopOf="@id/textViewDeparturesLabel" />

    <!-- Departures list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewDepartures"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:maxHeight="200dp"
        android:nestedScrollingEnabled="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewDeparturesLabel" />

    <!-- No departures message -->
    <TextView
        android:id="@+id/textViewNoDepartures"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/no_departures"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewDeparturesLabel" />

    <!-- Report delay button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/buttonReportDelay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/report_delay"
        app:icon="@drawable/ic_schedule"
        app:layout_constraintTop_toBottomOf="@id/recyclerViewDepartures" />

</androidx.constraintlayout.widget.ConstraintLayout>
